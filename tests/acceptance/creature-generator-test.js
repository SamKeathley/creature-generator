import { module, test } from 'qunit';
import { visit, currentURL } from '@ember/test-helpers';
import { setupApplicationTest } from 'ember-qunit';

module('Acceptance | creature generator', function(hooks) {
  setupApplicationTest(hooks);

  test('should show start button on the home page', async function (assert) {

  });
  test('should show random results', async function (assert) {

  });
  test('should show regenerate button', async function (assert) {

  });
  test('should regenerate all results', async function (assert) {

  });
  test('should generate new result for clicked word', async function (assert) {

  });
});
